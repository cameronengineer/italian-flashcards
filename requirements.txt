# Italian Flashcards — runtime dependencies.
# Pinned to the versions currently installed in .venv so re-running the
# pipeline is reproducible. Bump deliberately.

requests==2.34.1        # HTTP fallback (most calls use urllib in common.py)
genanki==0.13.1         # .apkg generation + deterministic GUIDs
Pillow==12.2.0          # PNG → JPEG image compression
elevenlabs==2.47.0      # ElevenLabs TTS client
